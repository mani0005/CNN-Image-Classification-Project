# CNN Image Classification

A complete **Convolutional Neural Network (CNN)** image classification project built using **TensorFlow / Keras**.  
This repository includes model training, saving, and inference using a trained CNN model.

---

## 📌 Project Overview

This project demonstrates how to:
- Build a CNN for image classification
- Train the model using TensorFlow/Keras
- Save the model using the modern `.keras` format
- Load the trained model for inference
- Run the project in a clean, reproducible environment

---

## Features

- CNN-based image classifier
- TensorFlow 2.13.1 + Keras
- Stable Python 3.10 environment
- Modern `.keras` model format
- Simple inference script (`app.py`)
- Clean GitHub-ready structure

---

## System Requirements

- Operating System: macOS / Linux / Windows
- Python **3.10** (required)
  
---

##Environment Setup (Recommended)

Use Conda to create an isolated environment:

conda create -n cnn python=3.10 -y
conda activate cnn

---

##Install all required Python packages:

pip install -r requirements.txt


##To run:
-python main.py
-streamlit run app.py
